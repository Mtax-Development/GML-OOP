function Draw(_sprite, _frame, _animationSpeed, _location, _scale, _angle, _color, _alpha) constructor
{
	sprite = _sprite;
	frame = _frame;
	animationSpeed = _animationSpeed;
	location = _location;
	scale = _scale;
	angle = _angle;
	color = _color;
	alpha = _alpha;
}

